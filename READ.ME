### important commands

1.npm init -y
2.npm install express
3.git init
4.git add.
if not email and user not config 
use git config user.email "EMAIL"
    git config user.name "USERNAME"
5.git commit -m init
6.`git push -u origin  main or master`
to check whether is main or master 
    git branch
to make remote as upstream 
    git push --set-upstream origin master
